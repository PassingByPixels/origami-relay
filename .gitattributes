# This folder is copied verbatim into a Linux-facing repository, so everything
# in it stays LF whatever platform the checkout is on. A carriage return in
# install.sh is a stray character at the end of every line of a running script.
* text=auto eol=lf
